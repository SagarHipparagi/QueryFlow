flask
flask-cors
pymysql
python-dotenv
# Core AI dependencies - removed meta 'langchain' package to avoid conflicts
langchain-community
langchain-openai
# Vector store and embeddings
faiss-cpu
sentence-transformers
# Application server
requests
gunicorn
# Protobuf is transitive, let pip resolve it
